{
    "name": "amplify-ci",
    "private": true,
    "devDependencies": {
      "@aws-amplify/backend-cli": "latest",
      "@aws-amplify/backend": "latest",
      "typescript": "latest"
    },
    "scripts": {
      "deploy": "ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID",
      "outputs": "ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID"
    }
  }
  